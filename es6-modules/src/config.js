// Default Export, can be named anything - can only be one per module
// export default apiKey;

// Named Export
export const apiKey = 'abc123';
export const url = 'https://www.sumonselim.com/wp-json/wp/v2';

export function sayHi(name) {
    console.log(`Hello there ${name}`);
}
